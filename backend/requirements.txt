# Pinned backend dependencies (verified: 51 pytest tests pass on Python 3.14).
# Regenerate with: /tmp/opencode/iks-venv/bin/pip freeze | grep -Ei "^(fastapi|uvicorn|pydantic|pytest|httpx)"
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
